name: flutter_test_example
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ">=2.17.6 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mockito: ^5.3.0                 #To simulate: db conecction without problems
  http: ^0.13.5       
  provider: ^6.0.3            
dev_dependencies:
  integration_test:               #add for integration test
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.0            #To use: $ flutter pub run  build_runner build --delete-conflicting-outputs
flutter:
  uses-material-design: true